[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "muse-sdk"
version = "0.1.0"
description = "MUSE Skill SDK — build skills for MUSE"
requires-python = ">=3.12"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["muse_sdk"]
